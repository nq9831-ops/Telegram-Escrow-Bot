<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.tg.escrow</groupId>
    <artifactId>telegram-escrow-bot</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>tgg-escrow</artifactId>
  <name>tgg-escrow</name>
  <description>担保交易：订单状态机 / 裁决多签规则 / 资金流水账。</description>

  <dependencies>
    <!-- 依赖方向单向：escrow 依赖 federation，反之不成立（否则成环）。
         federation 提供裁决所需的节点签名契约（FederationKeyPair 由
         FederationPartySignatureVerifier 消费）。

         刻意【不】依赖 tgg-core：escrow 目前不需要群管理的任何契约，而悬空依赖有真实成本
         ——传递依赖膨胀、构建顺序耦合、以及误导读者以为二者已经耦合。
         将来接线（Bot 层把命令与权限串起来）时若确实需要，再加不迟。 -->
    <dependency>
      <groupId>com.tg.escrow</groupId>
      <artifactId>tgg-common</artifactId>
    </dependency>
    <dependency>
      <groupId>com.tg.escrow</groupId>
      <artifactId>tgg-federation</artifactId>
    </dependency>

    <!-- 订单账本持久化：表结构由 Flyway 管理，JPA 侧 ddl-auto=validate -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- H2 只在测试期使用，跑真实 JPA 映射与 Flyway 迁移。
         不 mock 的理由：本层的错误形态是「列名对不上 / 类型不匹配 / 迁移没跑」，
         mock 全测不出来，只会在生产启动时炸。用 MySQL 兼容模式贴近生产方言。 -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
